# 2026-06-15 记忆

## 上午会话（7:58~9:45）- 节假日计划生成器优化与HTML报告

### 1. 节假日计划生成器格式优化
- 用户要求调整5题填空格式，解决钉钉显示换行问题
- 增加了空行、分隔线（`---`），使前场/后厨/裱花房模块更清晰
- 第5题后面新增提示语：**"🔥 其余事项我结合其他门店最佳实践帮你整理，你按输出调整即可，不用操心。✨"**（加粗+emoji，突出显示）
- 第4题格式优化：标题与内容之间加空行，每个小项之间换行更清晰
- 修改文件：`skills/holiday-plan-generator/references/questions.md`

### 2. HTML作战计划报告开发
- 基于顺昌店张翠媚店长实战案例，生成完整HTML报告
- 包含模块：目标卡片、时间线总览、人员排班、备货要点、前场作战、突发预案、预热动作、T-1筹备检查清单、作战日检查清单、复盘沉淀、店长口诀
- 设计特点：
  - 聚满分品牌色（品牌蓝#003CA5 + 橙色#EB6517）
  - 手机自适应布局
  - 可勾选清单（带checkbox框，可打印手写打勾）
  - 高亮关键信息（目标金额、时间点、数量）
  - T-1特别安排醒目提示（橙色边框）
  - 临时工特殊标记（橙色边框+橙色名字）
- 文件：`小北店_情人节作战计划.html`、`小北店_儿童节作战计划.html`

### 3. HTML截图发送钉钉功能
- 使用Chrome Puppeteer将HTML渲染为长截图
- 截图尺寸：414×3000像素（手机宽度，完整长度）
- 流程：用户回复5题填空 → 生成HTML → Chrome截图 → 直接发回钉钉
- 测试了情人节和儿童节两个版本，截图已发送给用户
- 技术问题：服务器8080端口被云服务器防火墙拦截，外部无法访问，改为截图方式更实用
- Chrome截图命令：`node -e "const p=require('puppeteer');(async()=>{...})()"`

### 4. 用户反馈与改进
- 用户确认截图在钉钉里显示效果"非常好"
- 用户希望最终实现：钉钉回复5题 → 自动生成HTML截图 → 直接发钉钉
- 整体输出结构清晰，方便读者一目了然
- 已充分结合张翠媚店长经验，无明显遗漏
- 格式已适配钉钉显示（无markdown表格、空行分隔、加粗关键信息）

## 待完善事项
- 截图高度可能需要根据内容动态调整（当前固定3000px，可能截不全或截太多空白）
- 需要考虑是否截成多张图（如果内容太长，单张图在钉钉里滑动体验可能不佳）
- 服务器端口8080需开放，或寻找其他方式提供HTML文件下载链接


## 上午会话（10:08~10:26）- 母亲节计划完整流程测试

### 1. 字体规格最终确定
- 正文：18px（原14px→16px→18px，用户确认"可以了"）
- 标题：22px
- 目标金额：44px
- 行高：1.9
- 截图DPI：2×（deviceScaleFactor=2）
- 确定为**标准模板字号**，以后所有节日计划固定使用

### 2. 技能流程更新（5步）
已更新 `skills/holiday-plan-generator/SKILL.md`：
1. **触发**：用户说"XX计划" → 发5题填空指引（最新版本）
2. **收集**：用户回复5题 → **先生成文字稿** → 发给用户确认
3. **确认**：用户确认"没问题"或"确认" → 生成HTML高清截图
4. **截图**：Chrome渲染（Puppeteer，deviceScaleFactor=2，viewport=414×12000，waitForNetworkIdle，端口9222）
5. **同步**：可选同步到钉钉AI表格（功能待开发）

**关键变更**：新增"文字稿确认"环节，避免截图后发现错误需要重生成。

### 3. 母亲节计划完整测试（南村店）
- 用户回复5题（真实数据）
- 生成文字稿 → 用户确认"确认"
- 生成HTML：`南村店_母亲节作战计划.html`
- Chrome截图 → 高清大字体（18px正文）
- 通过 `send_image_to_dingtalk` 发送到钉钉
- 用户确认流程满意

### 4. HTML截图技术细节（已标准化）
- Puppeteer端口：9222（避免启动新实例）
- 截图命令：`node -e "const p=require('puppeteer');(async()=>{const b=await p.connect({browserWSEndpoint:'ws://localhost:9222/devtools/browser/...'});const p2=await b.newPage();await p2.setViewport({width:414,height:12000,deviceScaleFactor:2});await p2.goto('file://...');await p2.waitForNetworkIdle();const e=await p2.$('body');const b2=await e.boundingBox();await e.screenshot({path:'...',clip:{x:0,y:0,width:414,height:b2.height}});await b.disconnect();})()"`
- 高度自动检测：通过 `element.boundingBox()` 获取body实际高度，避免截断或空白
- 图片大小：约1.2MB（1656×~10000像素）
- 跨渠道发送：使用 `send_image_to_dingtalk` 工具，将截图直接发到用户钉钉

### 5. 文件记录
- 母亲节HTML：`/root/.openclaw/workspace/南村店_母亲节作战计划.html`
- 截图：`/tmp/test_v7.png`（临时文件，每次生成新截图）
- 技能文件：`skills/holiday-plan-generator/SKILL.md`（已更新）
- 5题指引：`skills/holiday-plan-generator/references/questions.md`（已更新）

## 待完善事项
- 截图高度可能需要根据内容动态调整（当前使用boundingBox自动检测，已解决）
- 同步到钉钉AI表格功能：待开发（需接入API）
- 考虑增加"多门店批量生成"功能（如同时生成5个门店的计划）
